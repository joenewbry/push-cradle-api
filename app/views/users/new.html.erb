
<% provide(:title, "Signup for Less Mail") #used in application.html %>

<h1 id="notification_text"> No email notifications. No missed email. <br /> 
    Get a text when an email message needs your attention. </h1>

<!-- on md to lrg devices restrict the input size so the input-->

<div id="buttons-container" class="row"> 
	<div class="col-md-3"> </div>
	<div class="col-md-6"> 
		<%= bootstrap_form_for @user do |f| %>
		<%= f.text_field :name, label: "What do your friends call you?" %><br />
		<%= f.text_field :email, label: "What email should we monitor?"%> <br />
		<%= f.text_field :phone, label:  "What number should we text when you have an important email?"%><br />

		<%= f.submit "Start Your Free Trial", class: "btn btn-success center-block" %>
		<% end %>
	</div>
	<div class="col-md-3"> </div>
</div>